import 'package:flutter/material.dart';

class PlayersFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Players"),
    );
  }
}